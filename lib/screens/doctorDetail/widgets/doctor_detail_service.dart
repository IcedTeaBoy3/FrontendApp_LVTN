import 'package:flutter/material.dart';

class DoctorDetailService extends StatelessWidget {
  const DoctorDetailService({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
